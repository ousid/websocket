# Sample WebSocket ChatApp

#intialize the app:

composer install 

#start the server 

#in the command line just run the follow command:

php server.php

#press the connect in the front-end and send the messages!
